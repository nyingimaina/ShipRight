# .NET build artifacts
**/bin/
**/obj/
*.user
*.suo
.vs/
*.sln.docstates
*.cache

# .NET test results
**/TestResults/
*.trx
*.coverage

# Environment / secrets
.env
.env.local
.env*.local
*.pem
*.key

# Logs
*.log
logs/

# OS
.DS_Store
Thumbs.db

# Node / Next.js (covered per-directory by front-end/.gitignore, duplicated here as fallback)
node_modules/
.next/
out/
coverage/

# ShipRight runtime data (stored in ~/.shipright, not in repo)
# (no action needed — data dir is outside the repo)
